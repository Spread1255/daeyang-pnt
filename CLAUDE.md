# 작업 지침

- 이 프로젝트의 파일을 수정한 뒤에는 매번 사용자에게 묻지 말고 자동으로 `git add` → `git commit` → `git push origin main`까지 실행한다.
- 커밋 메시지는 그 turn에서 실제로 변경한 내용을 간결히 요약해서 작성한다.
- push가 실패하면(예: 인증/권한 오류) 조용히 넘어가지 말고 사용자에게 바로 알린다.
